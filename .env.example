# Server
PORT=3000

# Legacy sid decryption (aes-128-cbc), base64-encoded
SECRET_KEY=
IV=

# Upload cap for /v1/fileUpload (whole file is buffered in dyno RAM)
MAX_FILE_SIZE_MB=100

# Observability (optional — console-only when unset)
LOG_SERVICE_URL=
LOG_INGEST_KEY=
LOG_SAMPLE_RATE=1
